import {
	Observable,
	SchedulerLike,
	VirtualTimeScheduler,
	Subscription,
	Subject,
	empty,
	interval,
	from,
	of,
	pipe,
	race,
	MonoTypeOperatorFunction,
	OperatorFunction,
	PartialObserver,
	NEVER,
} from "rxjs";
import {
	debounce,
	debounceTime,
	tap,
	delay,
	map,
	mergeMap,
	ignoreElements,
	timeout,
	take,
	switchMap,
	takeLast,
	concatMap,
	flatMap,
	distinctUntilChanged,
	delayWhen,
	filter,
} from "rxjs/operators";

src.subscribe();
s.flush();
console.log(sampleHistory);
